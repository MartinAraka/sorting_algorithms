#include "sort.h"

/**
 * bubble_sort - sorts integer in ascending order
 * @array: array to be sorted
 * @size: no. of elements in the integer array
 */

void bubble_sort(int *array, size_t size)
{
	size_t i = 0;
	size_t j = 0;
	size_t tmp = 0;

	if (size <= 1)
		return;
}
